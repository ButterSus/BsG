package com.buttersus.gramutils

/**
 * A node that represents success node.
 * It is used as non-failure of the production.
 *
 * Usage:
 * ```
 * val `𝚗₁` = ::`∅`.`→`(…) // optional
 * println(`𝚗₁`) // Empty
 * ```
 */
interface NodeEmptyBase
